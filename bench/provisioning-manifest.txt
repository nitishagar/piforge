# Provisioning Manifest — Blocker 3 hallpi (ssnk@hallpi)
#
# Records every artifact this plan installs on the Pi, and the rollback step
# for each. Phase 6 executes the "Rollback" column. Baseline captured BEFORE
# any install so cleanup can verify return-to-baseline.
#
# Host: Raspberry Pi 5 Model B Rev 1.0, 8GB RAM, 850GB NVMe free.

## BASELINE (captured before provisioning)
# OS:       Linux hallpi 6.12.34+rpt-rpi-2712 aarch64 (Debian bookworm)
# Mem:      7.9Gi total, 2.2Gi used, 5.7Gi available
# Disk:     /dev/nvme0n1p2  918G  21G used  850G free (3%)
# Toolchain PRESENT:  git 2.39.5, cmake, gcc/g++, make
# Toolchain ABSENT:   rust/cargo, cactus, brew
# Repo:     no piforge clone

## ARTIFACTS INSTALLED (append as each lands)
1. Rust toolchain (~/.cargo, ~/.rustup)        Rollback: rm -rf ~/.cargo ~/.rustup
   - cargo 1.97.1, rustc 1.97.1
2. piforge clone (~/.piforge, blocker-3 branch) Rollback: rm -rf ~/piforge
   - commit 7a76ed1; rust/target/ build artifacts
3. Python 3.12.7 built from source              Rollback: sudo rm -f /usr/local/bin/python3.12*
   - installed to /usr/local (system python3 3.11 UNCHANGED)   and /usr/local/lib/python3.12, /usr/local/include/python3.12
   - source tree ~/Python-3.12.7 + ~/Python-3.12.7.tgz  (rm -rf ~/Python-3.12.7*)
4. cactus engine (~/.cactus)                    Rollback: rm -rf ~/cactus
   - cloned from github.com/cactus-compute/cactus
   - venv at ~/cactus/venv; cactus CLI on PATH when venv active
   - cactus serve / run / download / convert / clean all present

## CLEANUP (Phase 6) — COMPLETE, 2026-08-11
Disk returned to BASELINE (21G used, was 21G at start). All artifacts removed:
1. Rust toolchain: rm -rf ~/.cargo ~/.rustup (rustup self uninstall) ✓
2. piforge clone: rm -rf ~/piforge ✓
3. Python 3.12.7: sudo rm build dir + /usr/local/{bin,lib,include}/python3.12* ✓
4. cactus engine + clone + venv + all weights (qwen3-1.7b x2, needle): rm -rf ~/cactus ✓
5. HuggingFace hub cache (4.3G source weights): rm -rf ~/.cache/huggingface ✓
6. systemd units (cactus-serve, cactus-needle): stopped + removed + daemon-reload ✓
7. /tmp scripts + logs: removed ✓

RETAINED (negligible disk, generally-useful dev headers, part of /usr/include):
- apt build deps installed for Python+cactus: libssl-dev, zlib1g-dev, libffi-dev,
  libcurl4-openssl-dev, libsqlite3-dev, libbz2-dev, liblzma-dev, uuid-dev, etc.
  These are additive -dev packages; removing them is optional and they don't
  materially change the system (disk is exactly at baseline with them present).

VERIFIED: df -h shows 21G used (= baseline); python3 3.11.2 (system, untouched);
no cargo/rustc/cactus (baseline had none); no lingering processes; /tmp clean.
hallpi is GREENFIELD again, no worse than it started.
